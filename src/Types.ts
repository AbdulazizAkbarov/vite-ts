export type Student = {
    firstName: string;
    lastName: string;
    age: number;
    active: boolean;
    id: number;
  };

 export type Groups={
    id:number,
    firstName:string,
    active:boolean,
    students_count:number
  }